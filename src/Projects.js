import Fade from "react-reveal/Fade";

export default function Projects() {
    return (
        <div className={"page"} id={"projects"}>
            <Fade top>
                PROJECTS
            </Fade>
        </div>
    )
}